package primitives;
public class AreaOfTriangle {
    public static void main(String[] args) {

        /*
        find the area of a triangle if:
        base of the triangle is 6.8
        height of the triangle is 4.9

        Area = base * height / 2
         */
        double base = 6.8, height = 4.9, area;

        area = (base * height)/2; // Arithmetic operation/operators

        System.out.println(area + " is the area of the triangle");

    }
}
